NEST example networks
=====================

Some of the subdirectories contain collections of individual examples. Others contain a set of scripts of which only a single one is to be run. Those directories contain a file `run_example.py` to run the example.
